import React from 'react';

const Header = () => {
    return (
        <header className="app-header">
            <h1>RTSP Multi-Stream Dashboard</h1>

        </header>
    );
};

export default Header;
